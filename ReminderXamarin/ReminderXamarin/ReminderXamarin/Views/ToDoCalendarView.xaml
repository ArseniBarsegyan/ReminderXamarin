<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Name="ToDoCalendarViewPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:extensions="clr-namespace:ReminderXamarin.Extensions;assembly=ReminderXamarin"
             xmlns:base="clr-namespace:ReminderXamarin.ViewModels.Base;assembly=ReminderXamarin" xmlns:elements="clr-namespace:ReminderXamarin.Elements" xmlns:converters="clr-namespace:ReminderXamarin.Converters"
             x:Class="ReminderXamarin.Views.ToDoCalendarView"
             Title="{extensions:Translate ToDoPageTitle}"
             BackgroundColor="{DynamicResource ViewBackground}"
             base:ViewModelHelper.AutoWireViewModel="True">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ToDoStatusToColorConverter x:Key="ToDoStatusToColorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <AbsoluteLayout AbsoluteLayout.LayoutBounds="0,0,1,1"
                        AbsoluteLayout.LayoutFlags="All">
            <ListView x:Name="ToDoList"
                      AbsoluteLayout.LayoutBounds="0,0,1,1"
                      AbsoluteLayout.LayoutFlags="All"
                      SeparatorVisibility="None"
                      ItemsSource="{Binding AllModels}"
                      ItemSelected="ListViewOnItemSelected"
                      IsPullToRefreshEnabled="True"
                      RefreshControlColor="{DynamicResource RefreshControl}"
                      CachingStrategy="RecycleElement"
                      RefreshCommand="{Binding RefreshListCommand}"
                      IsRefreshing="{Binding IsRefreshing}">
                <ListView.Header>
                    <elements:ReminderCalendarView x:Name="ReminderCalendarView"
                                                   HeightRequest="400"
                                                   DaysWithActiveToDoInCurrentMonth="{Binding DaysWithActiveToDoInCurrentMonth}"
                                                   DaysWithCompletedToDoInCurrentMonth="{Binding DaysWithCompletedToDoInCurrentMonth}"
                                                   Command="{Binding SelectDayCommand}" />
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.ContextActions>
                                <MenuItem Text="Delete"
                                          IconImageSource="delete.png"
                                          Command="{Binding Source={x:Reference ToDoCalendarViewPage},
                                                            Path=BindingContext.DeleteToDoCommand}"
                                          CommandParameter="{Binding .}"
                                          IsDestructive="True" />
                            </ViewCell.ContextActions>

                            <ViewCell.View>
                                <Frame HasShadow="True"
                                       Padding="10,2"
                                       BackgroundColor="{DynamicResource CardViewBackground}"
                                       BorderColor="{DynamicResource CardViewBorder}"
                                       Margin="5,0">
                                    <StackLayout Orientation="Horizontal">
                                        <Label Text="{Binding Description}"
                                               VerticalOptions="Center">
                                            <Label.FontFamily>
                                                <OnPlatform x:TypeArguments="x:String">
                                                    <On Platform="iOS" Value="Roboto-Regular" />
                                                    <On Platform="Android" Value="Roboto-Regular.ttf#Roboto-Medium" />
                                                </OnPlatform>
                                            </Label.FontFamily>
                                        </Label>
                                        <BoxView HorizontalOptions="EndAndExpand"
                                                 VerticalOptions="Center"
                                                 BackgroundColor="{Binding Status, 
                                                         Converter={StaticResource ToDoStatusToColorConverter}}"
                                                 CornerRadius="10"
                                                 WidthRequest="20"
                                                 HeightRequest="20" />
                                    </StackLayout>
                                </Frame>                                
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ImageButton x:Name="CreateToDoButton"
                         Source="add.png"
                         Style="{StaticResource Fab}"
                         Command="{Binding CreateToDoCommand}">
                <ImageButton.CommandParameter>
                    <x:Int32>0</x:Int32>
                </ImageButton.CommandParameter>
            </ImageButton>

        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>